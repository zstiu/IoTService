package com.zstiu.IoTService.repository;

import com.zstiu.IoTService.domain.OrderDeviceCar;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OrderDeviceCarRepository extends JpaRepository<OrderDeviceCar, Long> {
}
